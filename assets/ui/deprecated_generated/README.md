# Deprecated Generated UI Assets

This folder keeps the old generated portraits, effects, and faction seals.
`gui/main.cpp` still loads it as a fallback after `assets/ui/bg3/` and the
legacy `assets/ui/generated/` path, so nothing here has been deleted.
